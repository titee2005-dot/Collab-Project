Audio assets are original procedural compositions created by scripts/generate-audio.mjs.
The application uses magical-theme.wav by default. Set src/data/audioConfig.js to
/audio/magical-theme.mp3 after adding your own licensed MP3. Optional effects use
the same configurable path convention. Missing audio disables playback gracefully.
