import {useEffect,useRef,useState} from 'react';
export default function AnimatedCount({value}){const [shown,setShown]=useState(value),previous=useRef(value);useEffect(()=>{const start=previous.current;if(value<=start||window.matchMedia?.('(prefers-reduced-motion: reduce)').matches){setShown(value);previous.current=value;return;}let frame;const at=performance.now();function tick(now){const t=Math.min(1,(now-at)/550),next=Math.round(start+(value-start)*(1-(1-t)**3));previous.current=next;setShown(next);if(t<1)frame=requestAnimationFrame(tick);}frame=requestAnimationFrame(tick);return()=>cancelAnimationFrame(frame);},[value]);return shown.toLocaleString();}
